import angular from 'angular';

import migrationNotification from './notification';

const moduleName = 'ngOvhMigrationComponents';

angular.module(moduleName, [migrationNotification]);

export default moduleName;
